{
  "name": "@aoeu/list",
  "version": "0.0.1",
  "description": "List and ListItem Components for rendering unordered lists",
  "main": "dist/index.js",
  "scripts": {
    "lint": "yarn run eslint && yarn run mdlint",
    "eslint": "eslint --ext .js --ext .jsx stories components",
    "mdlint": "markdownlint \"**/*.{md,mdx}\" --ignore \"node_modules/**\"",
    "test": "jest",
    "build": "webpack --config ../../webpack.config.js",
    "version": "standard-version"
  },
  "author": "The Art of Education University",
  "license": "MIT",
  "standard-version": {
    "skip": {
      "commit": true,
      "tag": true
    }
  }
}
